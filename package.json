{
  "name": "ton_workspace",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "pnpm -r run dev",
    "build": "pnpm -r run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": ">=22",
    "pnpm": ">=9"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",

  "note": "根目录的dependencies 和 devDependencies 可以被下面所有子项目共享",
  "dependencies": {
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "typescript": "^5.6.3",
    "eslint": "^9.14.0",
    "prettier": "3.3.3",
    "prettier-plugin-tailwindcss": "0.5.13"
  }
}
